sudo: required

services:
  - docker

before_install:
  - docker build -t laurence2013/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true laurence2013/docker-react npm run test -- --coverage
